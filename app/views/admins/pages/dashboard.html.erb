<!DOCTYPE html>
<html>
  <head>
    <title>Employee Recognition Platform</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application' %>
  </head>

  <body>
    <ul>
      <li>
  <p> <%= link_to 'Kudos listing page', admins_kudos_path, method: :get %></p>
      </li>
      <li>
  <p> <%= link_to 'Employees listing page', admins_employees_path, method: :get %></p>
      </li>
      <li>
  <p> <%= link_to 'Comapany values listing page', admins_company_values_path, method: :get %></p>
      </li>
      <li>
  <p> <%= link_to 'Rewards listing page', admins_rewards_path, method: :get %></p>
      </li>
      <li>
  <p> <%= link_to 'Deliveries', admins_employees_orders_path, method: :get %>(<%= Order.where(status:0).count%>)</p>
      </li>
      <li>
  <p> <%= link_to 'Add Kudos to all',  admins_add_kudos_to_all_path, method: :patch %></p>
      </li>
    </ul>
    <%= yield %>
  </body>
</html>
